query clubMembers($clubId: ID!) {
  clubMembers(clubId: $clubId) {
    Owner {
      ...UserAvatarData
    }
    Admins {
      ...UserAvatarData
    }
    Members {
      ...UserAvatarData
    }
  }
}
