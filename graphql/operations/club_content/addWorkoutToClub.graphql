mutation addWorkoutToClub($workoutId: ID!, $clubId: ID!) {
  addWorkoutToClub(workoutId: $workoutId, clubId: $clubId) {
    ...Club
    Owner {
      ...UserSummary
    }
    Admins {
      ...UserSummary
    }
    Members {
      ...UserSummary
    }
    ClubInviteTokens {
      ...ClubInviteToken
    }
    JoinClubInvites {
      ...JoinClubInvite
      Invited {
        ...UserSummary
      }
    }
    JoinClubRequests {
      ...JoinClubRequest
      Applicant {
        ...UserSummary
      }
    }
    Workouts {
      ...WorkoutData
    }
    WorkoutPlans {
      ...WorkoutPlanData
    }
  }
}
