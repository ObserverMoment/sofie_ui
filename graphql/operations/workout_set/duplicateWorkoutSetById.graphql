mutation duplicateWorkoutSetById($id: ID!) {
  duplicateWorkoutSetById(id: $id) {
    ...WorkoutSet
    WorkoutMoves {
      ...WorkoutMove
      Equipment {
        ...Equipment
      }
      Move {
        ...Move
      }
    }
  }
}
