query userCollectionById($id: ID!) {
  userCollectionById(id: $id) {
    ...Collection
    User {
      ...UserSummary
    }
    Workouts {
      ...WorkoutData
    }
    WorkoutPlans {
      ...WorkoutPlanData
    }
  }
}
