query userScheduledWorkouts {
  userScheduledWorkouts {
    ...ScheduledWorkout
    Workout {
      ...WorkoutSummary
    }
    GymProfile {
      ...GymProfile
      Equipments {
        ...Equipment
      }
    }
    loggedWorkoutId
    workoutPlanEnrolmentId
  }
}
