query workoutPlanEnrolmentById($id: ID!) {
  workoutPlanEnrolmentById(id: $id) {
    __typename
    WorkoutPlanEnrolment {
      ...WorkoutPlanEnrolment
      User {
        ...UserSummary
      }
    }
    WorkoutPlan {
      ...WorkoutPlanData
    }
  }
}
