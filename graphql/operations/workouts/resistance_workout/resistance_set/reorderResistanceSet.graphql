mutation reorderResistanceSet($id: ID!, $moveTo: Int!) {
  reorderResistanceSet(id: $id, moveTo: $moveTo) {
    ...ResistanceSet
    Move {
      ...Move
    }
    Equipment {
      ...Equipment
    }
  }
}
